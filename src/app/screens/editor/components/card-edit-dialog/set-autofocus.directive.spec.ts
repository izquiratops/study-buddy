import { SetAutofocusDirective } from './set-autofocus.directive';

describe('SetAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SetAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
