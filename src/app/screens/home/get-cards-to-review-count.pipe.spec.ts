import { GetCardsToReviewCountPipe } from './get-cards-to-review-count.pipe';

describe('GetCardsToReviewCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCardsToReviewCountPipe();
    expect(pipe).toBeTruthy();
  });
});
